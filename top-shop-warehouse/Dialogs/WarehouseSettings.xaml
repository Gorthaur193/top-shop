<Window x:Class="top_shop_warehouse.WarehouseSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WarehouseSettings" Height="200" Width="350"
        x:Name="This">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Current Warehouse:"  Margin="0, 0, 0, 5" />
            <ComboBox SelectedItem="{Binding CurrentWarehouse, ElementName=This, Mode=TwoWay}" 
                      ItemsSource="{Binding Warehouses, ElementName=This, Mode=OneWay}" />
        </StackPanel>
    </Grid>
</Window>